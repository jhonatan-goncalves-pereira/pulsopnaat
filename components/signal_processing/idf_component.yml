dependencies:
  # FFT real (dsps_fft2r) e janela de Hann (dsps_wind_hann) — componente
  # oficial ESP-IDF, otimizado (SIMD) para ESP32-S3. Declarado AQUI (e não em
  # main/) porque signal_processing é o consumidor direto; assim o projeto
  # principal e o test_app resolvem a dependência automaticamente.
  espressif/esp-dsp:
    version: "^1.6.0"
