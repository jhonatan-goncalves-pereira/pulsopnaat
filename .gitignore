# macOS
.DS_Store
.AppleDouble
.LSOverride

# Directory metadata
.directory

# Temporary files
*~
*.swp
*.swo
*.bak
*.tmp

# Log files
*.log

# Build artifacts and directories
**/build/
build/
*.o
*.a
*.out
*.exe # For any host-side utilities compiled on Windows

# ESP-IDF specific build outputs
*.bin
*.elf
*.map
flasher_args.json # Generated in build directory
sdkconfig.old
sdkconfig
sdkconfig.old.*

# ESP-IDF dependencies
# For older versions or manual component management
/components/.idf/
**/components/.idf/
# For modern ESP-IDF component manager
managed_components/
# If ESP-IDF tools are installed/referenced locally to the project
.espressif/

# --- Testes on-target (test_app) ---
test_app/build/
test_app/sdkconfig
test_app/sdkconfig.old

# --- Dependencies lock is generated; keep if you prefer pinning, ignore if not ---
# dependencies.lock

# CMake generated files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake

# --- CMake / ninja caches ---
*.ninja
.ninja_*

# Python environment files
*.pyc
*.pyo
*.pyd
__pycache__/
*.egg-info/
dist/

# Virtual environment folders
venv/
.venv/
env/

# Language Servers
.clangd/
.ccls-cache/
compile_commands.json

# Editor / OS cruft
.vscode/
.idea/

# Windows specific
Thumbs.db
ehthumbs.db
Desktop.ini

# User-specific configuration files
*.user
*.workspace # General workspace files, can be from various tools
*.suo       # Visual Studio Solution User Options
*.sln.docstates # Visual Studio

# --- Observabilidade local (stack MQTT/InfluxDB/Grafana) ---
# Segredos do compose nunca vão para o Git (ver .env.example)
.env
# Runtime do Mosquitto: banco de persistência e logs locais
mosquitto/data/*
!mosquitto/data/.gitkeep
mosquitto/log/*
!mosquitto/log/.gitkeep

# --- Scripts auxiliares locais e datasets capturados (não fazem parte do firmware) ---
_tools/
*.csv
# Exceção: dataset rotulado do ventilador da bancada é entregável (treino do regime_classifier)
!dataset/**/*.csv
