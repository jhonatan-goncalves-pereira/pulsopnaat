# --- ESP-IDF build artifacts ---
build/
sdkconfig.old
sdkconfig.old.*

# --- Testes on-target (test_app) ---
test_app/build/
test_app/sdkconfig
test_app/sdkconfig.old

# --- Managed components (restored via idf_component.yml + dependencies.lock) ---
managed_components/

# --- Dependencies lock is generated; keep if you prefer pinning, ignore if not ---
# dependencies.lock

# --- CMake / ninja caches ---
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
*.ninja
.ninja_*
compile_commands.json

# --- Editor / OS cruft ---
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# --- Python venvs / tooling (ESP-IDF uses Python) ---
.venv/
venv/
__pycache__/
*.pyc

*.log
